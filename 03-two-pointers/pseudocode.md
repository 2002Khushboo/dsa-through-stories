## Pseudocode
```
left = start
right = end

while left < right:
    if condition satisfied:
        record answer
        move one pointer
    else:
        move the pointer that causes the problem
```
